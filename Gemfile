source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins
gem 'webrick', '~> 1.8'

# Stdlib gems extracted from Ruby core in 3.4+/4.0 that the pinned Jekyll 3.9
# still requires. Needed only for local `bundle exec jekyll serve`; GitHub Pages
# builds with its own locked Ruby and ignores these.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
